# 🚀 SnapFolio - Modern Flexbox Portfolio

A high-performance personal portfolio website built to master **CSS Flexbox Architecture**. This project showcases a clean, dark-themed UI with complex structural alignments using pure HTML and CSS.

## 🛠️ Tech Stack
* **HTML5:** Semantic structure for SEO and accessibility.
* **CSS3:** Custom styling with advanced hover effects and transitions.
* **Flexbox:** 100% of the layout is driven by Flexbox (no floats or external frameworks like Bootstrap).

## 🌟 Key Features & Flexbox Implementation
* **Hero Section:** 50/50 split layout achieved using `display: flex` and `align-items: center`.
* **Professional Summary:** A clean "About Me" and "Stats" section aligned using flexible containers.
* **Skills Dashboard:** Progress bars and skill categories structured with `flex-direction: column`.
* **Service Cards:** A responsive-ready 3-column grid built with `flex-wrap: wrap` and `gap` properties.
* **Resume Timeline:** A vertical professional history layout utilizing nested flex containers.
* **Contact System:** A modern form layout with flexible input rows.



## 📂 Project Structure
```text
├── index.html          # Main HTML structure
├── src/
│   ├── css/
│   │   └── style.css   # Custom Flexbox styles
│   └── images/         # All portfolio and profile assets (.webp format)
